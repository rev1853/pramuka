{
  "name": "pramuka-quiz-host",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Real-time multiplayer Pramuka (Indonesian Scouting) quiz host",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"PORT=3000 npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "node server/index.js",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}